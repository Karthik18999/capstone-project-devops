apiVersion: v1
kind: Service
metadata:
  name: devops-service
spec:
  type: NodePort
  selector:
    app: devops
    version: green # <--- Changed to match your app-green deployment!
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30007
